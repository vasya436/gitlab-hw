# Домашнее задание к занятию  "Уязвимости и атаки на информационные системы"  Василий Ткаченко


### Задание 1

Скачайте и установите виртуальную машину Metasploitable: https://sourceforge.net/projects/metasploitable/.

Это типовая ОС для экспериментов в области информационной безопасности, с которой следует начать при анализе уязвимостей.

Просканируйте эту виртуальную машину, используя nmap.

Попробуйте найти уязвимости, которым подвержена эта виртуальная машина.

Сами уязвимости можно поискать на сайте https://www.exploit-db.com/.

Для этого нужно в поиске ввести название сетевой службы, обнаруженной на атакуемой машине, и выбрать подходящие по версии уязвимости.

Ответьте на следующие вопросы:

    Какие сетевые службы в ней разрешены?
    
В Metasploitable 2 по умолчанию открыты и работают следующие службы (типичные результаты Nmap):

    21/tcp - FTP (vsftpd)

    22/tcp - SSH (OpenSSH)

    23/tcp - Telnet

    25/tcp - SMTP (Postfix/Sendmail)

    53/tcp - DNS (BIND)

    80/tcp - HTTP (Apache)

    111/tcp - rpcbind

    139/tcp, 445/tcp - SMB (Samba)

    3306/tcp - MySQL

    5432/tcp - PostgreSQL

    5900/tcp - VNC

    6667/tcp - UnrealIRCd (IRC)

    1524/tcp - Ingreslock (бэкдор‑сервис)

    Какие уязвимости были обнаружены? (3 примера со ссылками)

    vsftpd 2.3.4 — бэкдор при логине с именем пользователя :)
    Эта уязвимость позволяет получить удалённый шелл без аутентификации.
    EDB‑ID: 49757
    Ссылка:  https://www.exploit-db.com/exploits/49757

    UnrealIRCd 3.2.8.1 — удалённое выполнение команд через специально сформированное сообщение
    Позволяет выполнить произвольные команды на сервере IRC.
    CVE: CVE‑2010‑2075
    EDB‑ID: 16922
    Ссылка: https://www.exploit-db.com/exploits/16922

    Порт 1524 (Ingreslock) — заранее подготовленный бэкдор
    В Metasploitable этот порт намеренно оставлен открытым и даёт прямой доступ к системе. Это не столько уязвимость ПО, сколько особенность образа для обучения.
    
Задание 2

Проведите сканирование Metasploitable в режимах SYN, FIN, Xmas, UDP.

Запишите сеансы сканирования в Wireshark.

Ответьте на следующие вопросы:

    Чем отличаются эти режимы сканирования с точки зрения сетевого трафика?
    Как отвечает сервер?

Приведите ответ в свободной форме.


    Чем отличаются эти режимы сканирования с точки зрения сетевого трафика?

Используемыми сетевыми протоколами, и флагами пакетов в случае TCP протокола:
SYN - TCP [SYN]
FIN - TCP [FIN]
Xmas - TCP [FIN, PSH, URG]

    Как отвечает сервер?

При UDP сканировании - ICMP пакетыми с кодом ошибок (порт закрыт) или UDP пакетом (порт открыт). Нет ответа - вероятно, порт открыт
SYN сканирование - TCP [SYN, ACK] (порт открыт) [RST, ACK], [RST] (порт закрыт) пакеты
FIN сканирование - TCP [RST, ACK] пакеты (порт закрыт)
Xmas сканирование - TCP [RST, ACK] пакеты (порт закрыт)


    

---


